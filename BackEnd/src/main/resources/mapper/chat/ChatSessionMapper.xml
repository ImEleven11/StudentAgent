<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uestc.studentagent.backend.chat.mapper.ChatSessionMapper">

    <resultMap id="ChatSessionMap" type="com.uestc.studentagent.backend.chat.entity.ChatSessionEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="session_type" property="sessionType"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findById" parameterType="string" resultMap="ChatSessionMap">
        SELECT id, user_id, session_type, status, created_at, updated_at
        FROM chat_sessions
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.uestc.studentagent.backend.chat.entity.ChatSessionEntity">
        INSERT INTO chat_sessions (
            id,
            user_id,
            session_type,
            status
        ) VALUES (
            #{id},
            #{userId},
            #{sessionType},
            #{status}
        )
    </insert>

    <delete id="deleteById" parameterType="string">
        DELETE FROM chat_sessions WHERE id = #{id}
    </delete>

</mapper>
