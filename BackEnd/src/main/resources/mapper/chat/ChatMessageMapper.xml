<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uestc.studentagent.backend.chat.mapper.ChatMessageMapper">

    <resultMap id="ChatMessageMap" type="com.uestc.studentagent.backend.chat.entity.ChatMessageEntity">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="structured_payload" property="structuredPayload"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findBySessionId" resultMap="ChatMessageMap">
        SELECT id, session_id, user_id, role, content, structured_payload, created_at
        FROM chat_messages
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countBySessionId" parameterType="string" resultType="long">
        SELECT COUNT(1)
        FROM chat_messages
        WHERE session_id = #{sessionId}
    </select>

    <insert id="insert" parameterType="com.uestc.studentagent.backend.chat.entity.ChatMessageEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_messages (
            session_id,
            user_id,
            role,
            content,
            structured_payload
        ) VALUES (
            #{sessionId},
            #{userId},
            #{role},
            #{content},
            #{structuredPayload}
        )
    </insert>

    <delete id="deleteBySessionId" parameterType="string">
        DELETE FROM chat_messages WHERE session_id = #{sessionId}
    </delete>

</mapper>
